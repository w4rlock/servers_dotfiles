# change key binding from C-b to Emacs style C-x
#set-option -g prefix C-a
#unbind-key C-b
#bind-key C-a send-prefix

set -g default-terminal "screen-256color"

# interface
#------------
# pane
#------------
set -g mouse-select-pane on
set -g pane-border-fg black
set -g pane-active-border-fg blue

#------------
# tabs
#------------
#setw -g window-status-format "#[fg=white]#[bg=blue] #I #[bg=blue]#[fg=white] #W "
#setw -g window-status-current-format "#[bg=brightmagenta]#[fg=white] *#I #[fg=white,bold]#[bg=cyan] [#W] "
#setw -g window-status-content-attr bold,blink,reverse

#------------
# status bar
#------------
set-option -g status-position top
set -g status-fg white
set -g status-bg blue
set -g status-left ''
set -g status-right-length 60
set -g status-right '♥#(acpi | cut -d ',' -f 2) | %a %m-%d %H:%M'

bind | split-window -h
bind - split-window -v
# END:panesplit

## moving between panes
## START:paneselect
bind h select-pane -L 
bind j select-pane -D 
bind k select-pane -U
bind l select-pane -R    
## END:paneselect

## Quick pane selection
## START:panetoggle
bind -r C-h select-window -t :-
bind -r C-l select-window -t :+
## END:panetoggle

## Pane resizing
bind -r H resize-pane -L 12
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5 
bind -r L resize-pane -R 12
## END:paneresize


set-option -g default-shell /bin/zsh


# set color for status bar
set-option -g status-bg colour235 #base02
set-option -g status-fg yellow #yellow
set-option -g status-attr dim 

# set window list colors - red for active and cyan for inactive
set-window-option -g window-status-fg brightblue #base0
set-window-option -g window-status-bg colour236 
set-window-option -g window-status-attr dim

set-window-option -g window-status-current-fg brightred #orange
set-window-option -g window-status-current-bg colour236 
set-window-option -g window-status-current-attr bright

# show host name and IP address on left side of status bar
set -g status-left-length 70
set -g status-left "#[fg=green]: #h : #[fg=brightblue]#(curl icanhazip.com) #[fg=yellow]#(ifconfig en0 | grep 'inet ' | awk '{print \"en0 \" $2}') #(ifconfig en1 | grep 'inet ' | awk '{print \"en1 \" $2}') #[fg=red]#(ifconfig tun0 | grep 'inet ' | awk '{print \"vpn \" $2}') "

# show session name, window & pane number, date and time on right side of
# status bar
set -g status-right-length 60
set -g status-right "#[fg=blue]#S #I:#P #[fg=yellow]:: %d %b %Y #[fg=green]:: %l:%M %p :: #(date -u | awk '{print $4}')::"
#set-option -g status-right '#(/Users/ramiro/.node/bin/tmux-mem --format ":currentBytes [#[fg=:color]:spark#[default]] #[fg=:color]:percent#[default]") #(/Users/ramiro/.node/bin/tmux-cpu --format ":load [#[fg=:color]:spark#[default]] #[fg=:color]:percent#[default]") %H:%M %d-%b-%y'
